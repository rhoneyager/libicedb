#include "../icedb/Table.hpp"
#include "../icedb/Group.hpp"

namespace icedb {
	namespace Tables {
		namespace impl {
			/// Hidden inner class that implements the actual table operations.
			/// The outer class is a template class, to match the datatype. The
			/// inner class is datatype invariant.
			class Table_impl {
			public:

			};
		}
	}
}