include(addlib)
#option (DEPS_OPTIONAL_EIGEN "Use provided Eigen3" ON)
set(DEPS_OPTIONAL_EIGEN ON)
if (DEPS_OPTIONAL_EIGEN)
	add_subdirectory(eigen3)
endif()
add_subdirectory(gsl-single)
#option (DEPS_OPTIONAL_MINIBALL "Use miniball" ON)
set (DEPS_OPTIONAL_MINIBALL ON)
if (DEPS_OPTIONAL_MINIBALL)
	add_subdirectory(miniball)
endif()
set(DEPS_OPTIONAL_QUICKHULL ON)
#option (DEPS_OPTIONAL_QUICKHULL "Use quickhull" ON)
if (DEPS_OPTIONAL_QUICKHULL)
	add_subdirectory(quickhull)
endif()
